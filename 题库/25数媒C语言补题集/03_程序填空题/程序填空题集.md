# 程序填空与读程序写结果题集（共 4 题）

> 本部分题目来自 PTA「25数媒C语言补题集」第三部分（题号 4-1 至 4-4）。  
> 答案与详细执行轨迹解析见同目录下 [`答案.md`](答案.md)。

---

### 4-1 判断素数与程序输出结果（2分）

**题目描述：**  
下列程序的输出结果为 `_______`。

```c
#include <stdio.h>
int fun3(int m);
int main(void)
{   
    int n;
    for (n = 1; n < 10; n++) 
        if (fun3(n) == 1) printf("%d ", n);                   /* %d后有1个空格 */
    return 0;
}
int fun3(int m)
{   
    int i;
    if (m == 2 || m == 3) return 1;     
    if (m < 2 || m % 2 == 0) return 0;
    for (i = 3; i < m; i = i + 2) 
        if (m % i == 0) return 0;
    return 1; 
}
```

---

### 4-2 各位数字有序性判断函数与声明填空（4分）

**题目描述：**  
输入若干个正整数，判断每个数从高位到低位各位数字是否按值从小到大排列，请根据题意，将程序补充完整。

```c
#include <stdio.h>
【空1】
int main(void)
{   
    int n;
    scanf("%d", &n);
    while (n > 0) {
        if(fun1(n)) 
            printf("%d中各位数字按从小到大排列\n", n);
        scanf("%d", &n);
    }
}
int fun1(【空2】)
{   
    int k;
    
    【空3】     
    while(m) 
        if( m / 10 % 10 > k) 
            return 0; 
        else {
            【空4】
            k = m % 10;
        }
    return 1;
}
```

---

### 4-3 函数参数与双循环格式控制（3分）

**题目描述：**  
下列函数输出一行字符：先输出 `kb` 个空格，再输出 `n` 个指定字符（由实参指定）。请根据题意，将程序补充完整。

```c
void print(【空1】)
{   
    int i;
    for(i = 1; i <= kb; i++)  
        【空2】
    for (【空3】) 
        printf("%c", zf);
}
```

---

### 4-4 static 静态局部变量与作用域生命周期（2分）

**题目描述：**  
下列程序的运行结果是 `_______`。

```c
#include <stdio.h>
int f(int x)
{  
    static int y = 1;
    x += y;  
    y++;
    return x;
}
int main(void)
{  
    int k;
    k = f(3);
    printf("%d %d\n", k, f(k));               //%d和%d之间只有1个空格
    return 0;
}
```
