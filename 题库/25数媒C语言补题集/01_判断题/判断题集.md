# 判断题集（共 10 题）

> 本部分题目来自 PTA「25数媒C语言补题集」第一部分（题号 1-1 至 1-10）。
> ✅ 可使用 `- [ ]` 进行勾选答题。答案与解析见同目录下 `答案.md`。

---

### 1-1 C语言标识符中，大小写字母是有区别的。（1分）

> C语言标识符中，大小写字母是有区别的。

- [x] 正确 (T)
- [ ] 错误 (F)

---

### 1-2 判断两个浮点数是否相同的运算虽然符合C语言规则，但是应该尽量避免。（1分）

> 判断两个浮点数是否相同的运算虽然符合C语言规则，但是应该尽量避免。

- [x] 正确 (T)
- [ ] 错误 (F)

---

### 1-3 在程序设计中，如果使用多个独立的 if 语句，程序只会执行其中满足条件的第一个 if 块，其余的即使条件为真也不会被执行。（1分）

> 这是一个判断题的样例。答案为T，分值为5分。

- [ ] 正确 (T)
- [x] 错误 (F)

---

### 1-4 在switch语句中，每一个的case常量表达式的值可以相同。（1分）

> 在switch语句中，每一个的case常量表达式的值可以相同。  ~@[](1)

- [ ] 正确 (T)
- [x] 错误 (F)

---

### 1-5 程序中的循环结构不仅能用于重复执行固定次数的代码段，也能根据条件动态调整循环次数。（1分）

> 这是一个判断题的样例。答案为T，分值为5分。

- [x] 正确 (T)
- [ ] 错误 (F)

---

### 1-6 在多层循环中, 一个break语句只向外跳一层。（1分）

> 在多层循环中, 一个break语句只向外跳一层。  ~@[](1)

- [x] 正确 (T)
- [ ] 错误 (F)

---

### 1-7 continue 不是结束本次循环，而是终止整个循环的执行。（1分）

> continue 不是结束本次循环，而是终止整个循环的执行。  ~@[](1)

- [ ] 正确 (T)
- [x] 错误 (F)

---

### 1-8 在while语句的循环体中，可以再嵌套do-while语句。（1分）

> 在while语句的循环体中，可以再嵌套do-while语句。

- [x] 正确 (T)
- [ ] 错误 (F)

---

### 1-9 运行包含以下代码段的程序将可能进入死循环。 （1分）

> 运行包含以下代码段的程序将可能进入死循环。 
```{c}
int i = 1；
while(i>0){ i++; printf("%d ",i); }
```

- [x] 正确 (T)
- [ ] 错误 (F)

---

### 1-10 在switch语句中，可以根据需要使用或不使用break语句。  （1分）

> 在switch语句中，可以根据需要使用或不使用break语句。  ~@[](1)

- [x] 正确 (T)
- [ ] 错误 (F)

---
