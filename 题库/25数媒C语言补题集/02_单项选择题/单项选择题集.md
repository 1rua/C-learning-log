# 单项选择题集（共 25 题）

> 本部分题目来自 PTA「25数媒C语言补题集」第二部分（单项选择题，共 25 题）。
> ✅ 每个选项使用 `- [ ]` 提供勾选。答案与深度解析见同目录下 `答案.md`。

---

### 2-1 数值溢出与类型转换截断（2分）

C语言代码段如下：
```c++
int i = 32777;
short si = i;
int j = si;
```
执行上述代码段后，`j` 的值是

- [ ] A. -32777
- [x] B. -32759
- [ ] C. 32759
- [ ] D. 32777

---

### 2-2 运算符优先级（1分）

在以下一组运算符中,优先级最高的运算符是()。

- [ ] A. <=
- [ ] B. =
- [x] C. %
- [ ] D. &&

---

### 2-3 逻辑关系表达式表示（2分）

能正确表示逻辑关系：“10≤x≤20” 的C语言表达式是（    ）。

- [ ] A. x>=10 and x<=20
- [ ] B. x>=10 or x<=20
- [x] C. x>=10 && x<=20
- [ ] D. x>=10 || x<=20

---

### 2-4 switch-case 穿透现象（Fall-through）（1分）

下列程序段的输出结果是 （）。   
```
int main(void)
{ 	
      int x = 1, a = 0, b = 0;
      
      switch(x)		
      {
            case 0:   b++;
            case 1:   a++;
            case 2:   a++; b++;	
      }
      printf("a=%d,b=%d\n",a,b);		

      return 0;
}

```

- [ ] A. a=2,b=1
- [ ] B. a=1,b=1
- [ ] C. a=1,b=0
- [x] D. a=2,b=2

---

### 2-5 if-else 就近匹配原则（1分）

在嵌套使用if语句时，C语言规定else总是（）。

- [ ] A. 和之前与其具有相同缩进位置的if配对
- [ ] B. 和之前与其最近的if配对
- [x] C. 和之前与其最近的且不带else的if配对
- [ ] D. 和之前的第一个if配对

---

### 2-6 while 循环与 continue 行为分析（2分）

下列程序段执行后`s`值为__。

```
int i=5, s=0;
while(i--)  
    if (i%2) continue; 
    else s+=i;
```

- [ ] A. 15
- [ ] B. 10
- [x] C. 9
- [ ] D. 6

---

### 2-7 嵌套循环与乘法表输出格式（1分）

以下程序段的输出结果是()。
int i, j;
for(i =1; i<4; i++)
{   for(j=i; j<4;j++)  printf("%d*%d=%d ", i, j, i*j);
printf("\n ");  }

- [ ] A. 1*1=1  1*2=2  1*3=3
2*1=2  2*2=4
3*1=3
- [x] B. 1*1=1  1*2=2  1*3=3
2*2=4  2*3=6
3*3=9
- [ ] C. 1*1= 1
1*2=2  2*2=4
1*3=3  2*3=6  3*3=9
- [ ] D. 1*1=1
2*1=2  2*2=4
3*1=3  3*2=6  3*3=9

---

### 2-8 循环控制 continue 语句（1分）

下面程序计算100之内能被3整除的整数之和。阅读程序，从4个选项中选出合适的选项填到【1】处，使程序功能完整。
```c
#include <stdio.h>
int main()
{
	int i, sum = 0;
	for (i = 1; i < 100; i++)
	{
		if (i % 3)
			【1】;
		sum += i;
	}
	printf("%d\n", sum);
	return 0;
}
```

- [ ] A. break
- [ ] B. return
- [x] C. continue
- [ ] D. return 0

---

### 2-9 for 循环终止条件与赋值运算陷阱（2分）

下面的这个循环的循环次数是（ ）。          
`for(int i=0,j=10; i=j=10; i++,j--)      `

- [x] A. 语法错误，不能执行
- [ ] B. 无限次
- [ ] C. 10
- [ ] D. 1

---

### 2-10 整数溢出与死循环（2分）

对下列代码的描述:

``` C
#include "stdio.h"
int main(){
    long num=1;
    while(num++ > 0) for(long i=num; i >=0; i--)printf("i :=\n \t %ld \n" , i);
    return 0;
}
```

正确的是：

- [ ] A. 程序有编译错误，不能执行
- [ ] B. 循环条件永为真，循环无限次，程序根本停不下来，打印输出无限行
- [x] C. 循环有限次，程序正常结束，打印输出有限行
- [ ] D. 程序运行过程必定会出错（崩溃），结果打印输出有限行

---

### 2-11 传统 C 语言函数的缺省返回类型（2分）

在C语言程序中，若对函数类型未加显式说明，则函数的隐含类型为（）。

- [x] A. void
- [ ] B. double
- [ ] C. char
- [ ] D. int

---

### 2-12 函数参数值传递（按值传参）（2分）

下列程序的输出结果是（）。 
```
fun(int a, int b, int c)
{   c = a * b;  }
int main(void)
{   
      int c;

      fun(2, 3, c);
      printf(“%d\n”, c);
	
      return 0;
}
```

- [ ] A. 0
- [ ] B. 1
- [ ] C. 6
- [x] D. 无法确定

---

### 2-13 模块化程序设计与函数目的（2分）

建立自定义函数的目的之一是（）。

- [ ] A. 提高程序的执行效率
- [x] B. 提高程序的可读性
- [ ] C. 减少程序的篇幅
- [ ] D. 减少程序文件所占内存

---

### 2-14 函数定义的正确语法规范（2分）

以下正确的函数定义形式是（）。

- [x] A. double fun(int x, int y)
- [ ] B. double fun(int x ; int y)
- [ ] C. double fun(int x, int y);
- [ ] D. double fun(int x, y)

---

### 2-15 形参与实参的区别（2分）

以下不正确的说法是（）。

- [ ] A. 实参可以是常量、变量或表达式
- [ ] B. 实参可以是任何类型
- [x] C. 形参可以是常量、变量或表达式
- [ ] D. 形参应与对应的实参类型一致

---

### 2-16 形参与实参的内存分配（2分）

以下正确的说法是（）。

- [ ] A. 实参与其对应的形参共同占用一个存储单元
- [ ] B. 实参与其对应的形参各占用独立的存储单元
- [x] C. 只有当实参与其对应的形参同名时才占用一个共同的存储单元
- [ ] D. 形参是虚拟的，不占用内存单元

---

### 2-17 while 与 do-while 核心区别（1分）

C语言中while和do-while循环的主要区别是（）。

- [x] A. do-while的循环体至少无条件执行一次
- [ ] B. while的循环控制条件比do-while的循环控制条件严格
- [ ] C. do-while允许从外部转到循环体内
- [ ] D. do-while的循环体不能是复合语句

---

### 2-18 break 语句的作用范围（1分）

下列叙述中正确的是（）。

- [ ] A. break 语句只能用于 switch 语句体中
- [ ] B. continue语句的作用是使程序的执行流程跳出包含它的所有循环
- [x] C. break 语句只能用在循环体内和 switch 语句体内
- [ ] D. 在循环体内使用 break语句和 continue 语句的作用相同

---

### 2-19 do-while 循环结束判定（1分）

下列叙述中正确的是（）。

- [ ] A. do-while语句构成的循环不能用其他语句构成的循环来代替
- [ ] B. do-while语句构成的循环只能用break语句退出
- [ ] C. 用do-while语句构成的循环，在while后的表达式为非零时结束循环
- [x] D. 用do-while语句构成的循环，在while后的表达式为零时结束循环

---

### 2-20 do-while 循环执行次数分析（1分）

执行以下循环语句时，下列说法正确的是（）。
```
x = -1; 
do { 
      x = x * x; 
} while (x == 0); 
```

- [ ] A. 循环体将执行一次
- [ ] B. 循环体将执行两次
- [x] C. 循环体将执行无限次
- [ ] D. 系统将提示有语法错误

---

### 2-21 循环累加的代数等价性（1分）

假设变量s、a、b、c均已定义为整型变量，且a、c均已赋值（c大于0），则与以下程序段功能等价的赋值语句是（）。   
```
s = a;  
for(b = 1; b <= c; b++) 
      s = s + 1; 
```

- [ ] A. s = a + b;
- [x] B. s = a + c;
- [ ] C. s = s + c;
- [ ] D. s = b + c;

---

### 2-22 辗转相减法求最大公约数（1分）

运行以下程序后，如果从键盘上输入65  14<回车>，则输出结果为（）。
```
int main(void)
{  
      int  m, n;

      printf("Enter m,n;");   
      scanf("%d%d", &m,&n);
      while (m != n)
      {   while (m > n)       m = m - n;
          while (n > m)       n = n - m;    
      }
      printf("m=%d\n",m);    

      return 0;
}
```

- [ ] A. m=3
- [ ] B. m=2
- [x] C. m=1
- [ ] D. m=0

---

### 2-23 循环中分支与 continue 打印轨迹（1分）

下列程序段的输出结果是（）。   
```
int main(void) 
{
      for(int i = 1; i < 6; i ++) {
            if( i % 2 != 0) { 
                  printf("#");
                  continue;  
            }
      printf("*");     
      }
      printf("\n");    
     
      return 0;
}
```

- [x] A. #*#*#
- [ ] B. #####
- [ ] C. *****
- [ ] D. *#*#*

---

### 2-24 while 循环变量整除更新（1分）

执行以下程序段后，变量` i `的值是（ ）。   
```
int i = 10;
while ( i > 3 ) {
    i /=2;
}
```

- [ ] A. 1
- [x] B. 2
- [ ] C. 3
- [ ] D. 5

---

### 2-25 do-while 循环步长累加（1分）

执行以下程序段后，变量`i`的值是（  ）

```
int i = 2;
do {
   i = i + 5;
} while (i < 15);
```

- [ ] A. 16
- [x] B. 17
- [ ] C. 20
- [ ] D. 21

---
