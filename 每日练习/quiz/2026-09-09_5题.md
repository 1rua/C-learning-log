# 2026-09-09 考前易错冲刺 5 题

1. 以下代码输出什么？
```c
int a = 1, b = 2;
if (a = 0) b++;
printf("%d, %d", a, b);
```
A. 1, 2  
B. 0, 2  
C. 0, 3  
D. 1, 3  

2. 表达式 `sizeof("abc\0de")` 的值是？  
A. 6  
B. 7  
C. 3  
D. 4  

3. 执行以下代码后，`x` 的值是？
```c
int a = 0, b = 2, c = 3;
int x = ++a || ++b && ++c;
```
A. 0  
B. 1  
C. 2  
D. 3  

4. 设 `int a[5] = {1, 2, 3, 4, 5}; int *p = a;`，则 `*(p + 2)` 和 `*p + 2` 的值分别是？  
A. 3 和 3  
B. 3 和 2  
C. 2 和 3  
D. 4 和 3  

5. 关于宏定义 `#define SQR(x) x * x`，若执行 `int a = SQR(2 + 3);`，则 a 的值是？  
A. 25  
B. 11  
C. 13  
D. 15  
