# C 语言学习进度报告

## 一、学习概览

- **统计时间**：2026-05-14 18:22
- **总题目数**：14 道
- **已复盘题目**：8 道
- **待复盘题目**：6 道（只写了代码但没有复盘文件）
- **总代码文件**：15 个
- **Git 提交次数**：未初始化 Git 仓库

---

## 二、最新学习轨迹（按时间倒序，最新在上）

### 最近完成：练习2-13 求N分之一序列前N项和
- **完成时间**：2025-05-13（根据复盘文件）
- **涉及知识点**：
  - `for` 循环遍历
  - `double` 双精度浮点数累加
  - C 语言整数除法与浮点除法的区别
  - `printf` 格式控制 `%.6f`
  - 变量初始化
  - `printf` 动态精度控制（`*` 占位符拓展）
- **掌握状态**：✅ 已掌握
- **主要收获**：理解了 C 语言整数除法的陷阱，浮点除法必须保证至少一个操作数是浮点类型；学会了 `printf("%.*f", m, sum)` 动态指定小数位数
- **易错点记录**：
  - 整数除法 `1/n` 结果为 0，必须写成 `1.0/n`
  - 局部变量必须手动初始化

---

### 上一题：练习2-12 输出华氏-摄氏温度转换表
- **完成时间**：2025-05-12
- **涉及知识点**：
  - `for` 循环的正确使用（循环变量、条件、增量）
  - C 语言**不支持**连续比较（`a <= b <= c` 是错误的）
  - `scanf`/`printf` 格式控制符
  - `printf` 宽度控制 `%6.1f`
  - 整数除法与浮点数除法的区别
  - 输入合法性判断
- **掌握状态**：⚠️ 需巩固
- **主要收获**：理解了循环三要素必须围绕同一个变量；C 语言不支持连续比较；`printf` 宽度控制可以精确对齐
- **易错点记录**：
  - 循环增量改变错变量，导致死循环/重复输出
  - 连续比较 `lower <= upper <= 100` 写法错误
  - 手动敲空格无法保证精确宽度，应使用 `%6.1f`
  - 整数除法 `5/9` 结果为 0

---

### 练习2-11 计算分段函数[2]
- **完成时间**：2025-05-11
- **涉及知识点**：
  - 分段函数的条件判断（`if-else`）
  - `double` 类型变量的使用
  - `math.h` 头文件及数学函数的调用
  - `scanf`/`printf` 对 `double` 的格式控制（`%lf` / `%f`）
- **掌握状态**：✅ 已掌握
- **主要收获**：掌握了 `math.h` 常用函数（`sqrt`、`pow`、`fabs` 等）；理解了 `sqrt(x)` 比 `pow(x, 0.5)` 语义更直观
- **易错点记录**：无，代码一次通过

---

### 练习2-10 计算分段函数[1]
- **完成时间**：2025-05-10
- **涉及知识点**：
  - `if-else` 条件分支结构
  - `double` 类型的输入输出格式控制
  - `scanf` 与 `printf` 格式控制符的区别
  - 浮点数运算中的类型转换
  - 分段函数的程序实现思路
- **掌握状态**：⚠️ 需巩固
- **主要收获**：明确了 `scanf` 读取 `double` 用 `%lf`，`printf` 输出 `double` 用 `%f`；`scanf` 不支持精度控制
- **易错点记录**：
  - `scanf` 使用了输出格式控制符 `%.1f`，导致读取失败
  - 整数除法 `1/x` 结果错误
  - `else` 后面多余分号
  - 输出缺少换行符

---

### 练习2-9 整数四则运算
- **完成时间**：2025-05-09
- **涉及知识点**：
  - `scanf` 读取多个变量
  - `printf` 格式化输出
  - 整数四则运算
  - 整数除法的截断特性
  - C89/C99 变量声明位置差异
- **掌握状态**：✅ 已掌握
- **主要收获**：理解了整数除法截断特性是核心考点；`scanf("%d %d")` 之间的空格可以跳过任意空白字符
- **易错点记录**：
  - `scanf` 格式字符串中使用了 `\n`，可能导致意外行为
  - 变量声明位置在 C89 中不兼容（代码在 C99 下通过）

---

### 计算摄氏温度
- **完成时间**：2025-05-08
- **涉及知识点**：
  - 变量的定义与使用
  - `scanf` 读取整数输入
  - `printf` 格式化输出
  - 基本的算术运算与整数除法
  - 严格匹配题目要求的输入输出格式
- **掌握状态**：⚠️ 需巩固
- **主要收获**：区分了 `%d`（格式控制符）和 `&`（取地址符）的不同作用
- **易错点记录**：
  - `scanf` 格式控制符写错，写成 `&d` 而不是 `%d`
  - 中文编码导致编译报错（UTF-8 vs GBK）
  - `LNK1168` 无法写入 .exe 文件（程序未关闭）

---

### 计算物体自由下落的距离
- **完成时间**：2025-05-07
- **涉及知识点**：
  - 自由落体物理公式的程序实现
  - 浮点数类型 `double` 的使用
  - `printf` 格式控制符 `%.2f`
  - 题目要求的精确理解
  - `math.h` 库函数的使用
- **掌握状态**：⚠️ 需巩固
- **主要收获**：深入理解了浮点类型（`float`/`double`/`long double`）的区别；掌握了 `math.h` 常用函数；理解了自动类型提升规则
- **易错点记录**：
  - 公式理解错误——计算了剩余高度而非下落距离
  - 引入了不必要的变量 `a = 100`
  - 变量类型选择错误（`int height` 导致小数截断）
  - `printf` 格式控制符错误（`%d` 输出浮点数）
  - 缺少换行符

---

### 输出倒三角图案
- **完成时间**：2025-05-06
- **涉及知识点**：
  - `for` 循环嵌套
  - `printf` 格式化输出
  - 空格和换行的精确控制
  - 循环变量的边界控制
- **掌握状态**：⚠️ 需巩固
- **主要收获**：掌握了循环嵌套的执行顺序；理解了 PTA 对输出格式的严格要求
- **易错点记录**：
  - `for` 循环分隔符错误（用逗号而不是分号）
  - 变量重复定义
  - `printf` 格式错误（缺少引号）
  - 语句末尾缺少分号
  - `return 0` 位置错误（写在函数外面）
  - 循环边界错误（`i <= 4` 导致输出 5 行）
  - 格式控制错误（第一个 `*` 前面多了空格）

---

## 三、知识点掌握情况汇总

### 已熟练掌握（出现多次且无错误）
- ✅ `printf` / `scanf` 基本输入输出
- ✅ `for` 循环基础结构
- ✅ `if-else` 条件分支
- ✅ 基本算术运算

### 已接触但需巩固（出现过错误或有疑问）
- ⚠️ **整数除法 vs 浮点除法**（在练习2-10、练习2-12、练习2-13、自由下落中犯错，共 4 次）
  - 表现：`1/2` 得到 0 而不是 0.5
  - 正确做法：确保至少一个操作数是浮点数 `1.0/2`
- ⚠️ **`scanf`/`printf` 格式控制符混淆**（在计算摄氏温度、分段函数[1]中犯错，共 2 次）
  - 表现：`scanf("&d")`、`scanf("%.1f")`、`printf("%d", double变量)`
  - 正确做法：`scanf` 用 `%d`/`%lf`，`printf` 用 `%f`/`%.nf`
- ⚠️ **变量初始化**（在练习2-13中犯错）
  - 表现：局部变量含有随机垃圾值
  - 正确做法：定义时赋初值
- ⚠️ **循环边界与循环变量**（在倒三角、温度转换表中犯错）
  - 表现：`i <= 4` 多输出一行、循环增量改变错变量
  - 正确做法：注意从 0 开始时 `< n` 对应 n 次
- ⚠️ **C 语言不支持连续比较**（在温度转换表中犯错）
  - 表现：`a <= b <= c` 被解析为 `(a <= b) <= c`
  - 正确做法：用逻辑与 `&&` 连接
- ⚠️ **输出格式严格匹配**（在倒三角、分段函数[1]中犯错）
  - 表现：多空格、少空格、缺少换行符
  - 正确做法：对照题目样例，完全一致再提交

### 尚未涉及
- ❌ 数组
- ❌ 函数（自定义函数）
- ❌ 指针
- ❌ 字符串
- ❌ 结构体
- ❌ 文件操作

---

## 四、易错点排查（基于复盘中的错误记录）

### 高频错误 Top 5

1. **整数除法陷阱**（出现 4 次）
   - 表现：`1/2` 得到 0 而不是 0.5，`5/9` 得到 0
   - 正确做法：确保至少一个操作数是浮点数 `1.0/2`

2. **`scanf`/`printf` 格式控制符错误**（出现 3 次）
   - 表现：`scanf("&d")`、`scanf("%.1f")`、`printf("%d", double)`
   - 正确做法：牢记 `scanf` 读 `double` 用 `%lf`，`printf` 输出用 `%f`

3. **输出格式不匹配**（出现 3 次）
   - 表现：多空格、少空格、缺少换行符
   - 正确做法：对照题目样例，用 `%6.1f` 等精确控制

4. **循环边界/变量错误**（出现 2 次）
   - 表现：`i <= 4` 多输出、循环增量改变错变量
   - 正确做法：从 0 开始时用 `< n`，确保三要素围绕同一变量

5. **变量未初始化/类型错误**（出现 2 次）
   - 表现：`int height` 截断小数、`double sum` 未初始化
   - 正确做法：需要小数时用 `double`，累加器定义时初始化为 0

---

## 五、待办与建议

### 待复盘题目（建议尽快补充复盘）
- 练习2-14 求奇数分之一序列前N项和（有代码无复盘）
- 练习2-15 求简单交错序列前N项和（有代码无复盘）
- 练习2-17 生成3的乘方表（有代码无复盘）
- 练习2-18 求组合数（有代码无复盘）
- 分段函数计算（函数题，有代码无复盘）
- Hello-world（有代码无复盘）

### 下一步学习建议
1. **补完上述待复盘题目的复盘文档** — 尤其是练习2-14、2-15、2-17，它们和已复盘题目知识点关联紧密
2. **重点巩固：整数除法** — 这是你犯错次数最多的知识点，每次涉及除法时先问自己"操作数是不是都是整数"
3. **重点巩固：格式控制符** — 牢记 `scanf %lf` vs `printf %f` 的区别
4. **继续完成当前章节剩余题目** — 练习2-18 求组合数涉及阶乘计算，是循环的进阶应用
5. **即将进入的新知识点**：数组（预计下一章）

---

## 六、学习曲线可视化（文字版）

```
第1周 [Hello-world] [输出倒三角图案] [温度转换]
第2周 [自由下落距离] [计算摄氏温度] [整数四则运算]
第3周 [分段函数[1]] [分段函数[2]] [华氏-摄氏温度转换表]
第4周 [求N分之一序列前N项和] [求奇数分之一序列] [求交错序列] [3的乘方表] [求组合数]
       ↑ 当前位置（待复盘：2-14、2-15、2-17、2-18）
```

---

## 七、鼓励与总结

你已经完成了 **14 道题目**，其中 **8 道** 做了详细复盘，覆盖了 C 语言基础输入输出、条件判断、循环、浮点数运算等核心知识点。从复盘质量来看，你对知识点的总结非常细致，尤其是自由下落那题的浮点类型详解和练习2-13的动态精度拓展，说明你在主动深入理解而不仅是做题。

**目前最需要注意的**：整数除法陷阱已经反复出现 4 次，这是 C 语言初学者最常见的坑，也是你和 Python 思维差异最大的地方。每次写除法时养成习惯——检查操作数类型。

**建议下一步**：先补完最近 4 道题的复盘，然后继续推进到数组章节。你的学习节奏很好，保持住！
